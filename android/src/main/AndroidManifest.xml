<?xml version="1.0" encoding="utf-8"?>
<!--
    AppAuth-Android ships its own `net.openid.appauth.RedirectUriReceiverActivity`
    in its AAR; manifest merging registers it in the host app automatically.
    Consumers must declare the redirect scheme via a `manifestPlaceholders` entry
    in `app/build.gradle.kts`, e.g. `appAuthRedirectScheme = "com.example.app"`.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
