/**
 * Public type surface for `@eurora-labs/tauri-plugin-appauth`.
 *
 * Stub kept intentionally minimal until phase 6 of the AppAuth rewrite lands;
 * see `plan.md` at the repo root for the target shape.
 */

export type AppAuthPluginVersion = '0.1.0';
